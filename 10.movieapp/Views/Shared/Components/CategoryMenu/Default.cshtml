@model IEnumerable<Category>

@foreach (var categories in Model)
{
    <div class="list-group">
        <a asp-controller="Home" asp-action="Index" asp-route-id="@categories.Id" class="list-group-item list-group-item-action ">@categories.Name</a>
    </div>
}
